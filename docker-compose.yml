version: '3.8'

services:
  dev:
    build: .
    container_name: xpilot-dev
    network_mode: bridge
    ports:
      - "1080:1080"  # SOCKS
      - "1087:1087"  # HTTP
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    command: /bin/bash
